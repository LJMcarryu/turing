{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Nuxt 3 App",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/app",
            "preLaunchTask": "npm: dev"
        }
    ]
}