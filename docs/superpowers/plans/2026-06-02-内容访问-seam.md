# 内容访问 seam 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把分散在 8 个页面与 RSS 路由的 `queryCollection` 取数，收敛成一个内容访问 seam——纯核（`shared/content/*`）+ client/server 两个适配。

**Architecture:** 纯核（`buildSpec`/`cacheKey`/`relatedPredicate`/`mergeFeed`/`applySpec`）无任何 Nuxt 依赖、可纯函数单测，放在 `shared/`（Nuxt 4 的 `#shared` 别名，app 与 server 同时可导入）。client 适配 = `useBlogPosts/useLearnArticles/useProjects` 等 composable（内部 `useAsyncData` + throw 500/404）；server 适配 = `rss.xml.ts`。两个适配坐实 seam。内容访问与图片解析**保持解耦**（见 `docs/adr/0001-内容访问与图片解析解耦.md`）。

**Tech Stack:** Nuxt 3.21（compatibilityVersion 4）· Vue 3 · @nuxt/content v3 · Vitest（happy-dom）· Playwright（e2e）

---

## 约定与命令

- 单测跑一次：`npx vitest run <file>`
- 类型检查：`npm run typecheck`
- Lint 修复：`npm run lint:fix`
- e2e：`npm run test:e2e`
- 纯核测试与源码**同目录**（`shared/content/*.test.ts`），用相对路径 import，无需改 `vitest.config.ts`。
- 适配层（composable / RSS）依赖 Nuxt 运行时，不做单测；以 `typecheck` + `e2e` 验证。

## 一处刻意的行为统一

现状 `app/pages/index.vue` 的 featured-learn 查询**没有** `order('date','DESC')`，而 learn 列表页有。本计划让 `buildLearnSpec`/`buildBlogSpec` **一律按 date DESC 排序**——这是刻意的一致性修正（featured 集合小、按日期排序合理且确定），迁移时 home 的 featured-learn 顺序可能从"插入序"变为"日期序"。`projects` 维持无排序（与现状一致）。

## File Structure

- `shared/content/spec.ts` — `QuerySpec` 类型、`buildBlogSpec/buildLearnSpec/buildProjectSpec`、`cacheKey`。纯。
- `shared/content/predicates.ts` — `relatedPredicate`、`mergeFeed`。纯。
- `shared/content/apply.ts` — `applySpec(builder, spec)` 泛型胶水 + `ChainBuilder` 接口。
- `shared/content/spec.test.ts` / `predicates.test.ts` / `apply.test.ts` — vitest。
- `app/composables/useBlogPosts.ts` — `useBlogPosts`、`getBlogPost`、`useRelatedPosts`。
- `app/composables/useLearnArticles.ts` — `useLearnArticles`、`getLearnArticle`、`useRelatedArticles`。
- `app/composables/useProjects.ts` — `useProjects`、`getProject`、`useOtherProjects`。
- 修改：`app/pages/**`（8 个页面）、`server/routes/rss.xml.ts`。

---

## Task 1: 纯核 — QuerySpec + buildSpec + cacheKey

**Files:**

- Create: `shared/content/spec.ts`
- Test: `shared/content/spec.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// shared/content/spec.test.ts
import { describe, expect, it } from 'vitest'
import { buildBlogSpec, buildLearnSpec, buildProjectSpec, cacheKey } from './spec'

describe('buildSpec', () => {
  it('blog 默认按 date DESC，无 where', () => {
    expect(buildBlogSpec()).toEqual({ collection: 'blog', order: ['date', 'DESC'] })
  })
  it('blog featured 进 where，limit 透传', () => {
    expect(buildBlogSpec({ featured: true, limit: 4 })).toEqual({
      collection: 'blog',
      order: ['date', 'DESC'],
      where: [['featured', true]],
      limit: 4,
    })
  })
  it('learn category + level 进 where 且按 date DESC', () => {
    expect(buildLearnSpec({ category: 'mcp', level: 'beginner' })).toEqual({
      collection: 'learn',
      order: ['date', 'DESC'],
      where: [
        ['category', 'mcp'],
        ['level', 'beginner'],
      ],
    })
  })
  it('projects 不排序，status 进 where', () => {
    expect(buildProjectSpec({ status: 'active' })).toEqual({
      collection: 'projects',
      where: [['status', 'active']],
    })
  })
  it('projects 默认无 where 无 order', () => {
    expect(buildProjectSpec()).toEqual({ collection: 'projects' })
  })
})

describe('cacheKey', () => {
  it('稳定且区分 opts', () => {
    expect(cacheKey(buildBlogSpec({ featured: true, limit: 4 }))).toBe(
      cacheKey(buildBlogSpec({ featured: true, limit: 4 }))
    )
    expect(cacheKey(buildBlogSpec({ limit: 4 }))).not.toBe(cacheKey(buildBlogSpec({ limit: 8 })))
    expect(cacheKey(buildLearnSpec({ category: 'mcp' }))).not.toBe(
      cacheKey(buildLearnSpec({ category: 'agent-development' }))
    )
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run shared/content/spec.test.ts`
Expected: FAIL — `Failed to resolve import "./spec"`。

- [ ] **Step 3: 实现 spec.ts**

```ts
// shared/content/spec.ts
// 内容查询的纯规格：把"查哪个集合、怎么排序/过滤/截断"表达成数据，
// 由 client(composable) 与 server(RSS) 两个适配各自执行。无任何 Nuxt 依赖。

export type Collection = 'blog' | 'learn' | 'projects'
export type OrderDir = 'ASC' | 'DESC'

export interface QuerySpec {
  collection: Collection
  order?: [field: string, dir: OrderDir]
  where?: Array<[field: string, value: string | number | boolean]>
  limit?: number
}

export interface BlogOpts {
  featured?: boolean
  limit?: number
}
export interface LearnOpts {
  category?: string
  featured?: boolean
  level?: 'beginner' | 'intermediate' | 'advanced'
  limit?: number
}
export interface ProjectOpts {
  status?: 'active' | 'wip' | 'archived'
  limit?: number
}

const DATE_DESC: [string, OrderDir] = ['date', 'DESC']

function compose(base: QuerySpec, where: QuerySpec['where'], limit?: number): QuerySpec {
  const spec: QuerySpec = { ...base }
  if (where && where.length) spec.where = where
  if (limit !== undefined) spec.limit = limit
  return spec
}

export function buildBlogSpec(opts: BlogOpts = {}): QuerySpec {
  const where: QuerySpec['where'] = []
  if (opts.featured !== undefined) where.push(['featured', opts.featured])
  return compose({ collection: 'blog', order: DATE_DESC }, where, opts.limit)
}

export function buildLearnSpec(opts: LearnOpts = {}): QuerySpec {
  const where: QuerySpec['where'] = []
  if (opts.category) where.push(['category', opts.category])
  if (opts.featured !== undefined) where.push(['featured', opts.featured])
  if (opts.level) where.push(['level', opts.level])
  return compose({ collection: 'learn', order: DATE_DESC }, where, opts.limit)
}

export function buildProjectSpec(opts: ProjectOpts = {}): QuerySpec {
  const where: QuerySpec['where'] = []
  if (opts.status) where.push(['status', opts.status])
  return compose({ collection: 'projects' }, where, opts.limit)
}

export function cacheKey(spec: QuerySpec): string {
  const o = spec.order ? `${spec.order[0]}:${spec.order[1]}` : ''
  const w = spec.where ? spec.where.map(([f, v]) => `${f}=${String(v)}`).join('&') : ''
  return ['content', spec.collection, o, w, spec.limit ?? ''].join('|')
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run shared/content/spec.test.ts`
Expected: PASS（8 个用例）。

- [ ] **Step 5: 提交**

```bash
git add shared/content/spec.ts shared/content/spec.test.ts
git commit -m "feat(content): add pure QuerySpec builders + cacheKey"
```

---

## Task 2: 纯核 — relatedPredicate + mergeFeed

**Files:**

- Create: `shared/content/predicates.ts`
- Test: `shared/content/predicates.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// shared/content/predicates.test.ts
import { describe, expect, it } from 'vitest'
import { mergeFeed, relatedPredicate } from './predicates'

const A = {
  path: '/blog/a',
  date: '2026-01-01',
  title: 'A',
  description: 'da',
  tags: ['ai', 'mcp'],
  category: 'x',
}
const B = {
  path: '/blog/b',
  date: '2026-02-01',
  title: 'B',
  description: 'db',
  tags: ['mcp'],
  category: 'x',
}
const C = {
  path: '/blog/c',
  date: '2026-03-01',
  title: 'C',
  description: 'dc',
  tags: ['vue'],
  category: 'y',
}

describe('relatedPredicate', () => {
  it('by tags：tag 重叠且排除自己', () => {
    const p = relatedPredicate(A, 'tags')
    expect(p(A)).toBe(false) // 自己
    expect(p(B)).toBe(true) // 共享 mcp
    expect(p(C)).toBe(false) // 无重叠
  })
  it('by category：同类且排除自己', () => {
    const p = relatedPredicate(A, 'category')
    expect(p(B)).toBe(true)
    expect(p(C)).toBe(false)
  })
  it('entry 无 tags 时 by tags 全 false', () => {
    expect(relatedPredicate({ path: '/x', tags: [] }, 'tags')(B)).toBe(false)
  })
})

describe('mergeFeed', () => {
  it('合并 + 按 date 降序 + 截断', () => {
    expect(mergeFeed([[A], [B, C]], 2).map(e => e.path)).toEqual(['/blog/c', '/blog/b'])
  })
  it('过滤掉缺字段的条目', () => {
    const bad = { path: '/blog/x', title: 'x' } // 缺 date/description
    expect(mergeFeed([[A], [bad]], 10).map(e => e.path)).toEqual(['/blog/a'])
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run shared/content/predicates.test.ts`
Expected: FAIL — `Failed to resolve import "./predicates"`。

- [ ] **Step 3: 实现 predicates.ts**

```ts
// shared/content/predicates.ts
// 取数后的纯函数：related 过滤、feed 合并。无 Nuxt 依赖。

export interface FeedLike {
  path?: string
  date?: string
  tags?: string[]
  category?: string
  title?: string
  description?: string
}

export function relatedPredicate<T extends FeedLike>(
  entry: T,
  by: 'tags' | 'category'
): (e: T) => boolean {
  const tags = entry.tags ?? []
  return (e: T) => {
    if (!e.path || e.path === entry.path) return false // 排除自己
    if (by === 'category') return !!entry.category && e.category === entry.category
    return !!e.tags && e.tags.some(t => tags.includes(t))
  }
}

export function mergeFeed<T extends FeedLike>(lists: T[][], limit: number): T[] {
  return lists
    .flat()
    .filter(e => e.title && e.description && e.date && e.path)
    .sort((a, b) => new Date(b.date!).getTime() - new Date(a.date!).getTime())
    .slice(0, limit)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run shared/content/predicates.test.ts`
Expected: PASS（5 个用例）。

- [ ] **Step 5: 提交**

```bash
git add shared/content/predicates.ts shared/content/predicates.test.ts
git commit -m "feat(content): add pure relatedPredicate + mergeFeed"
```

---

## Task 3: 薄胶水 — applySpec（假 builder 单测）

**Files:**

- Create: `shared/content/apply.ts`
- Test: `shared/content/apply.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// shared/content/apply.test.ts
import { describe, expect, it } from 'vitest'
import { applySpec, type ChainBuilder } from './apply'
import type { QuerySpec } from './spec'

// 假 builder：记录被调用的链式方法
function fakeBuilder() {
  const calls: string[] = []
  const b: ChainBuilder = {
    where: (f, op, v) => {
      calls.push(`where:${f}${op}${String(v)}`)
      return b
    },
    order: (f, d) => {
      calls.push(`order:${f}:${d}`)
      return b
    },
    limit: n => {
      calls.push(`limit:${n}`)
      return b
    },
  }
  return { b, calls }
}

describe('applySpec', () => {
  it('按 spec 依次施加 where/order/limit', () => {
    const { b, calls } = fakeBuilder()
    const spec: QuerySpec = {
      collection: 'learn',
      order: ['date', 'DESC'],
      where: [['category', 'mcp']],
      limit: 4,
    }
    expect(applySpec(b, spec)).toBe(b)
    expect(calls).toEqual(['where:category=mcp', 'order:date:DESC', 'limit:4'])
  })
  it('spec 缺项时不调用对应方法', () => {
    const { b, calls } = fakeBuilder()
    applySpec(b, { collection: 'projects' })
    expect(calls).toEqual([])
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run shared/content/apply.test.ts`
Expected: FAIL — `Failed to resolve import "./apply"`。

- [ ] **Step 3: 实现 apply.ts**

```ts
// shared/content/apply.ts
// 把 QuerySpec 套到任意"可链式 builder"上（client 的 queryCollection(name) 或
// server 的 queryCollection(event,name) 返回值）。只依赖链式接口 → 可用假 builder 单测。
import type { QuerySpec } from './spec'

export interface ChainBuilder {
  where: (field: string, op: '=', value: unknown) => ChainBuilder
  order: (field: string, dir: 'ASC' | 'DESC') => ChainBuilder
  limit: (n: number) => ChainBuilder
}

export function applySpec<B extends ChainBuilder>(builder: B, spec: QuerySpec): B {
  let b: ChainBuilder = builder
  if (spec.where) for (const [field, value] of spec.where) b = b.where(field, '=', value)
  if (spec.order) b = b.order(spec.order[0], spec.order[1])
  if (spec.limit !== undefined) b = b.limit(spec.limit)
  return b as B
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run shared/content/apply.test.ts`
Expected: PASS（2 个用例）。

- [ ] **Step 5: 全量纯核测试 + 提交**

Run: `npx vitest run shared/content/`
Expected: PASS（15 个用例）。

```bash
git add shared/content/apply.ts shared/content/apply.test.ts
git commit -m "feat(content): add type-preserving applySpec glue"
```

---

## Task 4: client 适配 — 三个 composable

**Files:**

- Create: `app/composables/useBlogPosts.ts`
- Create: `app/composables/useLearnArticles.ts`
- Create: `app/composables/useProjects.ts`

> 适配层依赖 Nuxt 自动导入（`useAsyncData`/`queryCollection`/`createError`/`Ref`），不做单测；本任务以 `npm run typecheck` 验证。

- [ ] **Step 1: 写 useBlogPosts.ts**

```ts
// app/composables/useBlogPosts.ts
import { applySpec } from '#shared/content/apply'
import { relatedPredicate } from '#shared/content/predicates'
import { type BlogOpts, buildBlogSpec, cacheKey } from '#shared/content/spec'

export async function useBlogPosts(opts: BlogOpts = {}) {
  const spec = buildBlogSpec(opts)
  const { data, error } = await useAsyncData(cacheKey(spec), () =>
    applySpec(queryCollection('blog'), spec).all()
  )
  if (error.value) throw createError({ statusCode: 500, message: 'Failed to load posts' })
  return { posts: data }
}

export async function getBlogPost(path: string) {
  const { data } = await useAsyncData(path, () => queryCollection('blog').path(path).first())
  if (!data.value) throw createError({ statusCode: 404, message: 'Not found' })
  return data
}

export function useRelatedPosts(post: Ref<{ path: string; tags?: string[] } | null>, limit = 3) {
  return useAsyncData(`related-${post.value?.path}`, async () => {
    if (!post.value?.tags?.length) return []
    const all = await applySpec(queryCollection('blog'), buildBlogSpec({ limit: 20 })).all()
    return all.filter(relatedPredicate(post.value!, 'tags')).slice(0, limit)
  })
}
```

- [ ] **Step 2: 写 useLearnArticles.ts**

```ts
// app/composables/useLearnArticles.ts
import { applySpec } from '#shared/content/apply'
import { relatedPredicate } from '#shared/content/predicates'
import { buildLearnSpec, cacheKey, type LearnOpts } from '#shared/content/spec'

export async function useLearnArticles(opts: LearnOpts = {}) {
  const spec = buildLearnSpec(opts)
  const { data, error } = await useAsyncData(cacheKey(spec), () =>
    applySpec(queryCollection('learn'), spec).all()
  )
  if (error.value) throw createError({ statusCode: 500, message: 'Failed to load articles' })
  return { articles: data }
}

export async function getLearnArticle(path: string) {
  const { data } = await useAsyncData(path, () => queryCollection('learn').path(path).first())
  if (!data.value) throw createError({ statusCode: 404, message: 'Not found' })
  return data
}

export function useRelatedArticles(
  article: Ref<{ path: string; category?: string } | null>,
  limit = 3
) {
  return useAsyncData(`related-${article.value?.path}`, async () => {
    if (!article.value?.category) return []
    const all = await applySpec(
      queryCollection('learn'),
      buildLearnSpec({ category: article.value.category, limit: 4 })
    ).all()
    return all.filter(relatedPredicate(article.value!, 'category')).slice(0, limit)
  })
}
```

- [ ] **Step 3: 写 useProjects.ts**

```ts
// app/composables/useProjects.ts
import { applySpec } from '#shared/content/apply'
import { buildProjectSpec, cacheKey, type ProjectOpts } from '#shared/content/spec'

export async function useProjects(opts: ProjectOpts = {}) {
  const spec = buildProjectSpec(opts)
  const { data, error } = await useAsyncData(cacheKey(spec), () =>
    applySpec(queryCollection('projects'), spec).all()
  )
  if (error.value) throw createError({ statusCode: 500, message: 'Failed to load projects' })
  return { projects: data }
}

export async function getProject(path: string) {
  const { data } = await useAsyncData(path, () => queryCollection('projects').path(path).first())
  if (!data.value) throw createError({ statusCode: 404, message: 'Not found' })
  return data
}

export function useOtherProjects(project: Ref<{ path: string } | null>, limit = 3) {
  return useAsyncData(`other-projects-${project.value?.path}`, async () => {
    const all = await applySpec(queryCollection('projects'), buildProjectSpec({ limit: 4 })).all()
    return all.filter(p => p.path !== project.value?.path).slice(0, limit)
  })
}
```

- [ ] **Step 4: 类型检查**

Run: `npm run typecheck`
Expected: 0 errors。若 `applySpec` 对真实 `queryCollection(...)` builder 报结构不兼容，则在 `apply.ts` 的 `ChainBuilder.where` 把 `value: unknown` 调整为与 @nuxt/content 一致的签名后重跑（仍保持泛型返回 `B`）。

- [ ] **Step 5: 提交**

```bash
git add app/composables/useBlogPosts.ts app/composables/useLearnArticles.ts app/composables/useProjects.ts
git commit -m "feat(content): add client composables over the content seam"
```

---

## Task 5: 迁移 blog（列表页 + 详情页）

**Files:**

- Modify: `app/pages/blog/index.vue`（`<script setup>` 取数块）
- Modify: `app/pages/blog/[slug].vue`（`<script setup>` 取数块）

- [ ] **Step 1: 改 blog/index.vue**

把现有：

```ts
const { data: posts, error } = await useAsyncData('blog-all', () =>
  queryCollection('blog').order('date', 'DESC').all()
)
if (error.value) throw createError({ statusCode: 500, message: t('error.loadFailed') })
```

替换为：

```ts
const { posts } = await useBlogPosts()
```

模板与 `allTags`/`filteredPosts` 计算属性不变（仍引用 `posts.value`）。

- [ ] **Step 2: 改 blog/[slug].vue**

把现有：

```ts
const route = useRoute()
const { data: post } = await useAsyncData(route.path, () =>
  queryCollection('blog').path(route.path).first()
)
if (!post.value) throw createError({ statusCode: 404, message: t('error.notFound') })
```

替换为：

```ts
const route = useRoute()
const post = await getBlogPost(route.path)
```

把现有 related 块：

```ts
const { data: relatedPosts } = await useAsyncData(`related-${route.path}`, async () => {
  if (!post.value?.tags?.length) return []
  const all = await queryCollection('blog').order('date', 'DESC').limit(20).all()
  return all
    .filter(
      p => p.path !== post.value!.path && p.tags?.some(tag => post.value!.tags?.includes(tag))
    )
    .slice(0, 3)
})
```

替换为：

```ts
const { data: relatedPosts } = useRelatedPosts(post)
```

模板（含 `heroImg`/`sideImg` 的 `useArticleImage` 调用）不变——图片保持解耦（ADR-0001）。

- [ ] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: 0 errors。

- [ ] **Step 4: e2e 验证 blog**

Run: `npm run test:e2e -- blog`
Expected: `e2e/blog.spec.ts` PASS。

- [ ] **Step 5: 提交**

```bash
git add app/pages/blog/index.vue app/pages/blog/[slug].vue
git commit -m "refactor(blog): use content seam composables"
```

---

## Task 6: 迁移 learn（列表 + 分类 + 详情）

**Files:**

- Modify: `app/pages/learn/index.vue`
- Modify: `app/pages/learn/[category]/index.vue`
- Modify: `app/pages/learn/[category]/[slug].vue`

- [ ] **Step 1: 改 learn/index.vue**

把：

```ts
const { data: articles, error } = await useAsyncData('learn-all', () =>
  queryCollection('learn').order('date', 'DESC').all()
)
if (error.value) throw createError({ statusCode: 500, message: t('error.loadFailed') })
```

替换为：

```ts
const { articles } = await useLearnArticles()
```

- [ ] **Step 2: 改 learn/[category]/index.vue**

把：

```ts
const { data: articles, error } = await useAsyncData(`learn-${category}`, () =>
  queryCollection('learn').where('category', '=', category).order('date', 'DESC').all()
)
if (error.value) throw createError({ statusCode: 500, message: t('error.loadFailed') })
```

替换为：

```ts
const { articles } = await useLearnArticles({ category })
```

- [ ] **Step 3: 改 learn/[category]/[slug].vue**

把：

```ts
const { data: article } = await useAsyncData(route.path, () =>
  queryCollection('learn').path(route.path).first()
)
if (!article.value) throw createError({ statusCode: 404, message: t('error.notFound') })
```

替换为：

```ts
const article = await getLearnArticle(route.path)
```

把 related 块：

```ts
const { data: relatedArticles } = await useAsyncData(`related-${route.path}`, async () => {
  const all = await queryCollection('learn')
    .where('category', '=', article.value!.category)
    .order('date', 'DESC')
    .limit(4)
    .all()
  return all.filter(a => a.path !== article.value!.path).slice(0, 3)
})
```

替换为：

```ts
const { data: relatedArticles } = useRelatedArticles(article)
```

- [ ] **Step 4: 类型检查 + e2e**

Run: `npm run typecheck && npm run test:e2e`
Expected: typecheck 0 errors；e2e 全绿。

- [ ] **Step 5: 提交**

```bash
git add app/pages/learn/index.vue "app/pages/learn/[category]/index.vue" "app/pages/learn/[category]/[slug].vue"
git commit -m "refactor(learn): use content seam composables"
```

---

## Task 7: 迁移 projects（列表 + 详情）

**Files:**

- Modify: `app/pages/projects/index.vue`
- Modify: `app/pages/projects/[slug].vue`

- [ ] **Step 1: 改 projects/index.vue**

把：

```ts
const { data: projects, error } = await useAsyncData('projects-all', () =>
  queryCollection('projects').all()
)
if (error.value) throw createError({ statusCode: 500, message: t('error.loadFailed') })
```

替换为：

```ts
const { projects } = await useProjects()
```

- [ ] **Step 2: 改 projects/[slug].vue**

把：

```ts
const route = useRoute()
const { data: project, error } = await useAsyncData(route.path, () =>
  queryCollection('projects').path(route.path).first()
)
if (error.value || !project.value)
  throw createError({ statusCode: 404, message: t('error.notFound') })
```

替换为：

```ts
const route = useRoute()
const project = await getProject(route.path)
```

把 other-projects 块：

```ts
const { data: otherProjects } = await useAsyncData(`other-projects-${route.path}`, async () => {
  const all = await queryCollection('projects').limit(4).all()
  return all.filter(p => p.path !== project.value!.path).slice(0, 3)
})
```

替换为：

```ts
const { data: otherProjects } = useOtherProjects(project)
```

- [ ] **Step 3: 类型检查 + e2e**

Run: `npm run typecheck && npm run test:e2e`
Expected: 全绿。

- [ ] **Step 4: 提交**

```bash
git add app/pages/projects/index.vue "app/pages/projects/[slug].vue"
git commit -m "refactor(projects): use content seam composables"
```

---

## Task 8: 迁移 home（index.vue 的三处取数）

**Files:**

- Modify: `app/pages/index.vue`

- [ ] **Step 1: 改三处 useAsyncData**

把：

```ts
const { data: featuredLearn } = await useAsyncData('featured-learn', () =>
  queryCollection('learn').where('featured', '=', true).limit(4).all()
)
const { data: latestBlog } = await useAsyncData('latest-blog', () =>
  queryCollection('blog').order('date', 'DESC').limit(4).all()
)
const { data: projects } = await useAsyncData('projects', () =>
  queryCollection('projects').limit(4).all()
)
```

替换为：

```ts
const { articles: featuredLearn } = await useLearnArticles({ featured: true, limit: 4 })
const { posts: latestBlog } = await useBlogPosts({ limit: 4 })
const { projects } = await useProjects({ limit: 4 })
```

> 注意：`featuredLearn` 现在按 date DESC 排序（见"刻意的行为统一"）。模板中 `featuredLearn`/`latestBlog`/`projects` 引用与 `useArticleImage` 调用不变。

- [ ] **Step 2: 类型检查 + e2e（home 暗场）**

Run: `npm run typecheck && npm run test:e2e -- home`
Expected: `e2e/home-dark.spec.ts` 与 `e2e/homepage.spec.ts` PASS。

- [ ] **Step 3: 提交**

```bash
git add app/pages/index.vue
git commit -m "refactor(home): use content seam composables"
```

---

## Task 9: server 适配 — RSS 用 applySpec + mergeFeed

**Files:**

- Modify: `server/routes/rss.xml.ts`

- [ ] **Step 1: 改取数块**

把：

```ts
import { queryCollection } from '@nuxt/content/server'
// ...
const learn = await queryCollection(event, 'learn').order('date', 'DESC').limit(20).all()
const blog = await queryCollection(event, 'blog').order('date', 'DESC').limit(20).all()

const allItems = [...learn, ...blog]
  .filter(hasFeedFields)
  .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())
  .slice(0, 20)
```

替换为：

```ts
import { queryCollection } from '@nuxt/content/server'
import { applySpec } from '#shared/content/apply'
import { mergeFeed } from '#shared/content/predicates'
import { buildBlogSpec, buildLearnSpec } from '#shared/content/spec'
// ...
const learn = await applySpec(queryCollection(event, 'learn'), buildLearnSpec({ limit: 20 })).all()
const blog = await applySpec(queryCollection(event, 'blog'), buildBlogSpec({ limit: 20 })).all()

const allItems = mergeFeed([learn, blog], 20)
```

> `mergeFeed` 已内含"过滤缺字段 + 按 date 降序 + 截断"，可删除文件中原有的 `hasFeedFields` 类型守卫与 `FeedItem` 接口（若不再被引用）。保留 `escapeXml`/`cdata`/`rssItems` 渲染逻辑不变。

- [ ] **Step 2: 类型检查 + 构建（确认 server 能解析 #shared）**

Run: `npm run typecheck && npm run build`
Expected: typecheck 0 errors；`nuxt build` 成功（prerender 含 `/rss.xml`）。

- [ ] **Step 3: 验证 RSS 内容**

Run: `npm run preview` 后另开终端 `curl -s http://localhost:3000/rss.xml | head -40`（或检查 `.output` 中预渲染的 `rss.xml`）。
Expected: 含 learn+blog 合并、按日期降序的 `<item>`，与迁移前一致。

- [ ] **Step 4: 提交**

```bash
git add server/routes/rss.xml.ts
git commit -m "refactor(rss): consume shared content seam (second adapter)"
```

---

## Task 10: 收尾 — 验证无残留直呼 + 全量校验

**Files:** 无（验证 + 收尾提交）

- [ ] **Step 1: 确认页面不再直呼 queryCollection**

Run: `grep -rn "queryCollection(" app/pages`
Expected: 0 处。若有遗漏，回到对应任务迁移。

- [ ] **Step 2: 全量校验**

Run: `npx vitest run && npm run typecheck && npm run lint && npm run test:e2e`
Expected: 单测全绿（15 用例）、类型 0 错、lint 0 错、e2e 全绿。

- [ ] **Step 3: 删除计划残留（可选）**

确认 `server/routes/rss.xml.ts` 中 `hasFeedFields`/`FeedItem` 若已无引用则删除（Step 1 的 lint 会提示未使用）。

- [ ] **Step 4: 收尾提交**

```bash
git add -A
git commit -m "chore(content): finish content seam migration"
```

---

## Self-Review（计划自检）

- **Spec 覆盖**：纯核（spec/predicates/apply）✓ Task 1-3；client 适配 ✓ Task 4；8 页面迁移 ✓ Task 5-8；RSS server 适配 ✓ Task 9；related/feed 收编 ✓（`useRelatedPosts`/`useRelatedArticles`/`useOtherProjects`/`mergeFeed`）；404/500 收进模块 ✓；cacheKey 纯核 ✓；解耦图片 ✓（ADR-0001 + 模板不动）。
- **类型一致**：`buildBlogSpec/buildLearnSpec/buildProjectSpec`、`cacheKey`、`applySpec`、`relatedPredicate`、`mergeFeed`、`ChainBuilder`、`QuerySpec` 在 Task 1-3 定义，Task 4/9 一致引用；composable 返回名 `{ posts }`/`{ articles }`/`{ projects }` 与页面模板变量一致。
- **占位符扫描**：无 TBD/TODO；每个代码步含完整代码与确切命令。
- **风险点**：Task 4 Step 4 已标注 `applySpec` 对真实 builder 的结构兼容性回退方案。
